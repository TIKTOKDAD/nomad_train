# ============================================================
# Core package - runtime, config, checkpoint, and registry helpers
# ============================================================
# core 子包放置训练基座的基础设施代码，不直接依赖具体算法。
# 核心模块入口：运行时、配置、注册表与检查点相关工具
"""Core runtime, config, registry, engine, and checkpoint helpers."""
